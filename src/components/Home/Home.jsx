import { Container } from './Home.styled.jsx'

export const Home = () => {
    return (
        <section>
            <Container>
                павп
                {/* {loading && <Loader />}
                {error && <p>{error}</p>}
                {movies.length > 0 && <MovieList monies={movies} />} */}
            </Container>
        </section>
  );
        
}